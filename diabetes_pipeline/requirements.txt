# diabetes_pipeline — Python dependencies
# Matches PySpark 4.0.2 used in GCP/Colab runs (Python 3.12+)
# NOTE: Java 17+ must be installed separately (pyspark does not manage JVM)
#
# Setup:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r diabetes_pipeline/requirements.txt

pyspark==4.0.2

# Jupyter — needed to run .ipynb notebooks locally
notebook>=7.0
ipykernel>=6.0

# Visualization — diabetes_pipeline/visualization/visualizer.ipynb
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
numpy>=1.26
pyarrow>=15.0
